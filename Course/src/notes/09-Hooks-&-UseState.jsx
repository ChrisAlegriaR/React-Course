// ^Hooks y Estados.
// ^Antes de la versión 16.8 de React, se utilizaban principalmente los **Class-Based Components**, los cuales son los componentes de tipo clase que vimos anteriormente. Debido a esto, existía una diferencia bastante importante en la manera de manejar el estado de una aplicación, ya que dentro de los componentes de clase era necesario utilizar `this` para hacer referencia al propio componente y acceder o modificar propiedades como `this.state` y `this.setState()`. Esto hacía que el manejo del estado fuera un poco más complejo y que los componentes tuvieran una estructura más extensa y difícil de comprender, especialmente cuando comenzaban a tener mucha lógica. Todo esto cambió considerablemente a partir de la versión **16.8 de React**, cuando se introdujeron los **Hooks**, permitiendo utilizar estado y otras funcionalidades de React directamente dentro de componentes funcionales sin necesidad de utilizar componentes de clase. Actualmente, los componentes funcionales junto con los Hooks son la forma recomendada y más utilizada para construir aplicaciones modernas con React. Los Hooks son funciones especiales proporcionadas por React que permiten utilizar características internas de React, como el estado, efectos secundarios, memorización y otras funcionalidades, dentro de componentes de tipo función. Entre los Hooks propios de React podemos encontrar `useState`, `useEffect`, `useMemo`, `useCallback`, `useContext`, entre muchos otros. Además de los Hooks proporcionados por React, también tenemos la posibilidad de crear nuestros propios **Custom Hooks**, los cuales son funciones reutilizables que permiten encapsular lógica específica para utilizarla en diferentes componentes de una manera más sencilla, legible y organizada. Por lo que es realmente fundamental entender un concepto base: dentro de una aplicación existen datos, eventos y variables que pueden cambiar a lo largo del tiempo, pero dichos cambios inicialmente solamente ocurren en la memoria mediante JavaScript. React, por otro lado, es el encargado de tomar esos datos y utilizarlos para construir y mostrar la interfaz de usuario. Por ejemplo, podemos modificar una variable en memoria mediante una operación de JavaScript, pero si React no sabe que dicha variable cambió, la información que aparece en pantalla continuará mostrando el valor anterior. Aquí es donde entran los Hooks y, específicamente, los estados. Los Hooks permiten conectar la lógica de JavaScript con lo que React muestra en el frontend, haciendo posible que cuando determinados datos cambien, React vuelva a renderizar o "repinte" la parte correspondiente de la interfaz para mostrar la información actualizada. Por esta razón, un estado en React puede entenderse como información que pertenece a un componente y cuyo cambio puede provocar una nueva renderización de la interfaz. Estos estados representan datos que la interfaz necesita para funcionar correctamente y pueden cambiar durante la interacción del usuario. En los componentes funcionales, los estados se manejan principalmente mediante Hooks como `useState`, mientras que en los antiguos componentes de clase se utilizaban mecanismos como `state` y `setState`. De esta manera, React puede saber qué información cambió y actualizar la interfaz de manera eficiente, evitando que nosotros tengamos que modificar manualmente el DOM.
// ~UseState.
// ~Como bien se hizo mención anteriormente, los Hooks son funciones especiales que nos permiten utilizar diferentes características de React dentro de nuestros componentes funcionales, permitiendo actualizar, modificar o interactuar con la información que React utiliza para pintar nuestra página web. En el caso específico del Hook `useState`, este nos permite agregar un **estado** a una variable dentro de un componente funcional. Cuando declaramos una variable utilizando `useState` y posteriormente utilizamos dicha variable dentro del JSX que nuestro componente retorna, React puede conocer la relación existente entre ese estado y la interfaz. De esta manera, cuando el valor del estado cambia utilizando la función proporcionada por `useState`, React detecta dicho cambio y vuelve a renderizar el componente para mostrar en la página web la información actualizada. Esto es importante porque una variable normal de JavaScript, aunque sea utilizada dentro del JSX, no provoca automáticamente una nueva renderización cuando su valor cambia. React no está observando constantemente todas las variables existentes en memoria para detectar sus modificaciones, por lo que simplemente cambiar una variable normal no es suficiente para actualizar la interfaz. `useState` soluciona precisamente este problema, ya que crea una relación entre el valor almacenado y el proceso de renderizado de React. Además, `useState` devuelve un arreglo con dos elementos: el primero representa el valor actual del estado y el segundo es una función especial que React proporciona para actualizar dicho estado. Cuando utilizamos esta segunda función para modificar el valor, React sabe que el estado cambió y programa una nueva renderización del componente utilizando el nuevo valor. Por esta razón, para modificar un estado nunca debemos intentar cambiar directamente la variable, sino que debemos utilizar siempre la función `set` correspondiente. También es indispensable importar el Hook `useState` desde la librería de React antes de utilizarlo dentro del componente, ya que `useState` es una funcionalidad proporcionada por React y debe estar disponible dentro del archivo. A continuación se muestra la estructura básica para utilizar `useState` dentro de un componente funcional de React.
// ?Importación de useState.
// ?Para poder utilizar un Hook, y en este caso específicamente `useState`, debemos importarlo desde la librería de React. Para ello utilizaremos la palabra reservada `import` y entre llaves `{}` indicaremos que queremos importar `useState`, utilizando posteriormente `from 'react'`. Esto se debe a que `useState` es un Hook propio de React y no necesitamos instalar ninguna librería adicional para utilizarlo. Una vez realizada esta importación, podremos utilizar `useState` dentro de nuestros componentes funcionales. Por lo tanto, la implementación completa para importar este Hook será la siguiente:
import { useState } from "react"; //* Se importa el Hook useState desde la librería React para poder utilizar estados dentro del componente.

// ?Declaración de variable con useState.
// ?Una vez importado `useState`, podremos declarar las variables cuyo cambio queremos que React detecte para posteriormente volver a renderizar la interfaz. Para ello utilizaremos una declaración de variable un tanto especial. Normalmente se utiliza `const` y, dentro de corchetes `[]`, declararemos dos elementos separados por una coma. El primero será el nombre que tendrá nuestra variable o estado, mientras que el segundo será el nombre de la función que React nos proporciona para modificar dicho estado. Generalmente, se utiliza el mismo nombre de la variable acompañado del prefijo `set`, por ejemplo `contador` y `setContador`, aunque esto es únicamente una convención de nombres y no una obligación. Posteriormente, igualaremos este arreglo a la ejecución de `useState()`, colocando los paréntesis porque `useState` es una función. Dentro de estos paréntesis especificaremos el **valor inicial** que tendrá nuestro estado. Este valor puede ser prácticamente cualquier tipo de dato que necesitemos, como un Number, String, Boolean, Array, Object, etc. En este caso utilizaremos `0` como valor inicial para nuestro contador.
const [contador, setContador] = useState(0); //* Se crea el estado contador con valor inicial 0 y se obtiene setContador para actualizarlo.

// ?Utilización de variables y su set.
// ?Como vimos anteriormente, toda variable declarada mediante `useState` normalmente se declara utilizando `const`. A primera vista esto puede resultar extraño, ya que sabemos que las variables declaradas con `const` no pueden recibir directamente una nueva asignación. Sin embargo, en el caso de `useState` no modificamos directamente la variable `contador`, sino que utilizamos la segunda posición del arreglo, es decir, la función `setContador`, que React nos proporciona específicamente para actualizar el estado. Por esta razón podemos tener `const contador` y aun así cambiar su valor a través de `setContador`. No estamos reasignando directamente la constante, sino indicándole a React cuál debe ser el nuevo valor del estado. Además, la función `setContador` no solamente modifica el valor que tenemos almacenado en memoria, sino que también informa a React de que el estado cambió, provocando que el componente vuelva a renderizarse y que la interfaz muestre el nuevo valor. Esto permite mantener sincronizados los datos que tenemos en memoria con los datos que visualizamos en pantalla. Por ejemplo, dentro de una función podemos utilizar `setContador(contador + 1)` para tomar el valor actual de `contador`, sumarle uno y establecer el resultado como el nuevo estado. React detectará esta actualización y volverá a ejecutar el componente para mostrar el nuevo valor. Es importante recordar que no debemos modificar directamente el estado utilizando algo como `contador = contador + 1`, ya que esto no le indicaría correctamente a React que debe actualizar la interfaz. Siempre que queramos modificar un estado debemos utilizar la función `set` correspondiente.
//* Componente funcional que utiliza un estado para almacenar y actualizar un contador.
function NombreComponente() { //* Se declara el componente funcional donde utilizaremos useState.
    const [contador, setContador] = useState(0); //* Se crea el estado contador con un valor inicial de 0 y su función para actualizarlo.

    const incrementar = () => { //* Se declara una función que será utilizada para incrementar el valor del contador.
        setContador(contador + 1); //* Se actualiza el estado sumando 1 al valor actual y React volverá a renderizar el componente.
    }; //* Finaliza la función incrementar.

    return ( //* Se retorna el contenido visual del componente.
        <> //* Fragment permite agrupar los elementos sin agregar un nodo adicional al DOM.
            <h1>{contador}</h1> //* Se muestra en pantalla el valor actual del estado contador.
            <button onClick={incrementar}>Preciona el boton.</button> //* Al hacer clic se ejecuta incrementar y se actualiza el estado.
        </> //* Finaliza Fragment.
    ); //* Finaliza el return.
} //* Finaliza el componente.

export { NombreComponente }; //* Se exporta el componente para poder utilizarlo desde otros archivos.

// ?Igualacion o asignacion de variable useState mediante otra variable.
// ?Dentro de React, debemos tener mucho cuidado con la forma en la que actualizamos una variable administrada mediante `useState`. Si bien en JavaScript puro estamos acostumbrados a utilizar el operador `=` para asignar directamente un nuevo valor a una variable, con una variable creada mediante `useState` **no debemos modificar directamente su valor utilizando una asignación tradicional**. En su lugar, React nos proporciona una segunda función, comúnmente conocida como función `set`, que es la encargada de actualizar el estado y notificar a React que dicho valor ha cambiado, provocando posteriormente un nuevo renderizado del componente. Por lo tanto, si tenemos un estado como `[nombre, setNombre]`, no debemos intentar realizar algo como `nombre = nombre2`, ya que estaríamos intentando modificar directamente el estado. Para actualizarlo debemos utilizar `setNombre(valorNuevo)`. Ahora bien, es perfectamente válido que el nuevo valor provenga de otra variable; por ejemplo, podemos tener `const nombre2 = 'Pedro'` y posteriormente llamar `setNombre(nombre2)`. En este caso no estamos igualando directamente el estado con `=`, sino que estamos **utilizando la función setter para indicarle a React cuál debe ser el nuevo valor**. Esto es precisamente lo importante de este ejemplo. Además, debemos tener mucho cuidado con el lugar donde ejecutamos el `set`, ya que una actualización de estado provoca un nuevo renderizado y, si llamamos al setter directamente durante el cuerpo del componente en cada renderizado, React volverá a ejecutar el setter, provocando nuevamente otro renderizado y pudiendo generar un **ciclo de renderizado infinito**. Por esta razón, aunque podemos pasar otra variable como argumento al setter, dicha actualización normalmente debe realizarse como consecuencia de un evento, un efecto o alguna otra condición controlada. Para este caso, únicamente es necesario utilizar la función `set` correspondiente al estado y pasarle como argumento la variable que contiene el nuevo valor. El setter se encargará de actualizar internamente el estado y React será quien administre el nuevo valor y el renderizado correspondiente. Por lo tanto, no es necesario ni recomendable modificar directamente la variable que representa el estado mediante `=`, ya que React necesita controlar ese estado mediante la función setter.
function Igualacion() { //* Se declara el componente funcional Igualacion.
    const [nombre, setNombre] = useState('jesus'); //* Se declara el estado nombre con su función setNombre y se establece "jesus" como valor inicial.

    const nombre2 = 'Pedro'; //* Se declara una variable independiente que contiene el nuevo valor que queremos utilizar.

    setNombre(nombre2); //* Se utiliza la función setter para actualizar el estado nombre utilizando el valor almacenado en nombre2.
    //* **Importante:** aunque esta forma utiliza correctamente el setter, ejecutarlo directamente dentro del cuerpo del componente hará que React intente actualizar el estado durante cada renderizado y puede provocar un ciclo de renderizado infinito.
    //* **Buena práctica:** las actualizaciones de estado normalmente deben realizarse mediante eventos, efectos o alguna condición que controle cuándo debe ejecutarse el setter.

    return ( //* Se retorna el contenido JSX del componente.
        <> //* Fragment utilizado para agrupar el contenido.
            <p>{nombre}</p> //* Se muestra el valor actual del estado nombre dentro del DOM.
        </> //* Fin del Fragment.
    ); //* Fin del return.
} //* Fin del componente.

export { Igualacion }; //* Se exporta el componente mediante una exportación nombrada.